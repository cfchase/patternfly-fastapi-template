apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-proxy
  labels:
    app: app
  annotations:
    # For OpenShift OAuth, add the redirect URI annotation
    # serviceaccounts.openshift.io/oauth-redirecturi.primary: "https://your-app.example.com/oauth2/callback"
